import * as React from 'react'
import { Block } from 'baseui/block'
import { Label } from '@dakan/ui'

const Examples = ({ dataset_id }) => {
  return (
    <React.Fragment>
      <Label>Ikke implementert enda</Label>
    </React.Fragment>
  )
}
export default Examples
